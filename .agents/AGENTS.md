# Restart Application
When working on the Flutter app, always kill the running app and start it again after making code changes. Do not rely exclusively on hot reload unless it's a very minor change or the user permits it. The user explicitly prefers a full restart (kill and re-run) after modifications to ensure everything works smoothly.
