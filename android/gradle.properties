org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
kotlin.jvm.target.validation.mode=WARNING
android.useAndroidX=true
# R8 in ProGuard-compatibility mode. Full mode is the default and shrinks a
# little more, but it honours keep rules more narrowly — a kept class keeps its
# no-argument constructor here and does not there — which is the shape of the
# launch crash that had R8 turned off (see app/proguard-rules.pro). Drop this
# line after a release has proven itself on devices if the extra shrinking is
# wanted.
android.enableR8.fullMode=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
